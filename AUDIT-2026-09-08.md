# ผลตรวจ My Key — 8 กันยายน 2026

ข้อสรุป: ฟังก์ชันจดและเปิดรหัสผ่านทำงานจริง แต่ยังไม่ควรใช้เป็นที่เก็บรหัสสำคัญเพียงแห่งเดียว จนกว่าจะแก้การกู้คืน การนำเข้า และการป้องกัน PIN

## ขอบเขตและหลักฐาน

- อ่านโค้ด React, crypto, localStorage, Firebase, หน้าจอหลัก และ Android configuration ภายในแอปนี้ ไม่พบ AGENTS.md ในตำแหน่งบรรพบุรุษที่ตรวจหรือในแอป
- Local HEAD และ GitHub main ตรงกัน: 0fdc058c680027e6c5b4990c6f2b5fc868244554
- GitHub Actions deploy ล่าสุดสำเร็จ: https://github.com/snoopy1989doo-source/My-Key/actions/runs/33822644307
- เปิดเว็บจริง https://snoopy1989doo-source.github.io/My-Key/ ได้ หน้าเริ่มต้นแสดงปกติ
- ทดสอบ interaction บน localhost:4317 ด้วยข้อมูลสมมติ ใน browser viewport ที่ตรวจภาพได้จริง 390 × 844 ไม่ใช่การทดสอบ touch/keyboard ของโทรศัพท์จริง
- adb devices -l ไม่พบอุปกรณ์หรือ emulator ที่เชื่อมต่อ จึงยังไม่ยืนยัน APK, Android lifecycle, การดาวน์โหลด/แชร์ไฟล์ใน WebView และการใช้แบบ offline บนมือถือจริง
- Build ผ่านด้วย npm run build -- --outDir .audit-build; bundle JS 850.98 kB (gzip 248.67 kB) พร้อมคำเตือนขนาด bundle
- รัน lint แล้ว มี warnings รวมถึงการไล่ตรวจไฟล์ build ใน www; ไม่มี test script ใน package.json
- ทดสอบ logic จริงของ VaultProvider ผ่าน harness ที่จำลอง React state/ref และ localStorage ในหน่วยความจำ ปิด cloud ใน harness ไม่ใช่ integration test ของ React lifecycle
- ไม่แก้ source code, APK หรือ deploy งานใหม่ รายการสมมติที่บันทึกผ่าน UI ใช้ Firebase anonymous session แยกของ browser ทดสอบ และแอปแสดงเวลาซิงก์สำเร็จ ไม่ได้ตรวจอ่าน document กลับจาก server

## สิ่งที่ทดสอบผ่าน

| รายการ | หลักฐาน |
|---|---|
| สร้าง vault, เพิ่มและแก้ไขรายการภาษาไทย, บันทึก username/password/PIN/notes | ผ่าน UI |
| ค้นหาที่พบและไม่พบรายการ | ผ่าน UI |
| แสดงและซ่อน password/PIN | ผ่าน UI |
| Reload แล้วล็อก, PIN ผิดถูกปฏิเสธ, PIN ถูกเปิดข้อมูลเดิมได้ | ผ่าน UI |
| ล็อกด้วยปุ่มแล้วปลดล็อกด้วย Master Password | ผ่าน UI; ปุ่มมีปัญหาตำแหน่งในจอแคบ |
| สุ่มรหัสใหม่ | ผ่าน UI |
| AES-GCM encryption/decryption, ปฏิเสธ ciphertext ที่แก้ไข | ผ่าน isolated logic test |
| ถอดรหัสด้วย PIN, Master และ Recovery Key ที่ทราบค่า | ผ่าน isolated logic test; หน้าแสดง Recovery Key มีบั๊ก |
| สำรอง meta/vault แล้วนำกลับและถอดรหัสรายการเดิม | ผ่าน isolated logic test; ไม่ใช่การทดสอบดาวน์โหลด/เลือกไฟล์บน Android |

## ปัญหาที่ควรแก้ก่อนใช้จริง

### 1. Recovery Key ถูกข้ามหลังสร้าง vault — สูง

UI ทดสอบสร้างเสร็จแล้วเข้า list ทันที ไม่มีโอกาสบันทึกกุญแจฉุกเฉิน
src/context/VaultContext.jsx:171 ตั้ง isSetup=true และ isLocked=false ทำให้ src/App.jsx:26 เปลี่ยนออกจาก SetupScreen ก่อนขั้น setEmergencyKey/setStep(2) ที่ src/components/SetupScreen.jsx:62 จะอยู่ให้ผู้ใช้ใช้งาน
แก้: แยกสถานะสร้าง vault กับยืนยันเก็บ recovery key และอย่า unmount จนยืนยันเสร็จ เพิ่มวิธีสร้าง Recovery Key ใหม่โดยยืนยันตัวตน สำหรับ vault เดิมที่พลาดขั้นตอนนี้

### 2. เปลี่ยนเครื่องแล้วกู้จาก Cloud ตาม README ไม่ได้ — สูง

src/services/firebase.js:42 ใช้ anonymous authentication และเก็บ document ตาม user.uid (:90, :116) ไม่มี flow ล็อกอินบัญชีถาวรหรือเชื่อม anonymous account เดิม บนอุปกรณ์ใหม่จะไม่มีตัวตนเดิม อีกทั้ง downloadVault (:109) ไม่มีผู้เรียกใน UI และหน้า Setup มีแต่สร้าง vault ใหม่ ไม่มีทางกู้จากไฟล์ตั้งแต่แรก
แก้: เพิ่มบัญชีถาวร/การเชื่อมบัญชี และปุ่ม Restore จาก cloud/ไฟล์บนหน้าแรก ทดสอบสอง session อิสระจริงก่อนอ้างว่าย้ายเครื่องได้ แสดงความแตกต่างระหว่าง upload สำเร็จกับกู้คืนได้ หลีกเลี่ยงคำรับรองข้อมูลไม่หาย 100%
อ้างอิง Firebase: https://firebase.google.com/docs/auth/web/anonymous-auth

### 3. ไฟล์สำรองและ cloud มีทางปลดล็อกด้วย PIN สั้น — สูง

src/context/VaultContext.jsx:138–160 ใส่ pinSalt และ wrappedByPin ใน meta ทั้งชุด และ export/sync meta ทั้งชุดด้วย ผู้ที่ได้ไฟล์หรือ encrypted blob ไปจึงสามารถลอง PIN แบบ offline ได้ ไม่ต้องเดา Master Password; PIN 4 หลักมีเพียง 10,000 ค่า และ UI ไม่มีการหน่วงตามจำนวนครั้งผิด การเพิ่มตัวนับใน UI อย่างเดียวแก้ offline guessing ไม่ได้
แก้: ไม่ใส่ PIN-wrapped key ใน portable backup/cloud ใช้ Master/Recovery ห่อ key สำหรับข้อมูลที่ส่งออก ส่วน quick unlock ผูกกับ Android Keystore และการยืนยันตัวตนในเครื่อง พร้อมวิธีย้ายข้อมูลรุ่นเก่า
PBKDF2 ปัจจุบัน 120,000 รอบ ควรทบทวน KDF พร้อม benchmark มือถือและ versioned migration; OWASP ยก PBKDF2-HMAC-SHA256 600,000 รอบเป็นแนวทาง แต่เพิ่มรอบอย่างเดียวไม่ทำให้ PIN สั้นปลอดภัยเพียงพอ
อ้างอิง: https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html

### 4. ไฟล์สำรองเสียทับข้อมูลเดิมได้ — สูง

src/context/VaultContext.jsx:478 ตรวจเพียงว่ามี meta และ vault แล้วเขียนทับทันที ยืนยันด้วย isolated test ว่า {"meta":{},"vault":{}} ได้ success=true และ metadata เดิมถูกทับ
แก้: ตรวจ app/version/schema/ขนาด และทดลองถอดรหัสไฟล์ใหม่ก่อน commit เก็บสำเนาข้อมูลเดิมและให้เลือก merge หรือ replace แยกกัน สำรอง custom categories ด้วย (ปัจจุบัน export ไม่มี settings/categories)

### 5. บันทึกลงเครื่องล้มเหลวแต่ผู้ใช้ไม่รู้ — สูง

src/services/storage.js:67 catch ข้อผิดพลาด localStorage แล้วแค่ console.error; saveVaultItem แสดงข้อมูลใหม่ก่อนยืนยัน persist ผู้ใช้อาจเห็นรายการบนจอแต่หายเมื่อเปิดใหม่ ยืนยันด้วย simulated QuotaExceededError ว่า setter ไม่โยน error ให้ผู้เรียก
แก้: คืนข้อผิดพลาดให้ UI และยืนยันการบันทึกก่อนแสดงสำเร็จ เก็บ metadata/payload ให้เป็นชุดเดียวหรือ transaction เพื่อไม่ให้ข้อมูลคู่กันไม่ตรง

### 6. ปุ่มซิงก์ทันทีใน Settings ใช้ไม่ได้ — กลางถึงสูง

src/components/SettingsModal.jsx:265 ใช้ onClick={triggerCloudSync} ทำให้ React click event ถูกส่งเป็น metaOverride ไป Firestore ทดสอบ UI ได้ error: Unsupported field value: a custom SyntheticBaseEvent object (found in field meta)
แก้: onClick={() => triggerCloudSync()} และแสดงสถานะ error/เวลา sync สำเร็จจริง ทั้ง Header และ Settings ไม่ควรใช้แค่มี config เป็นหลักฐานเชื่อมต่อสำเร็จ ปุ่ม cloud ที่ Header เป็น wrapper ถูกต้องอยู่แล้ว จึงไม่ควรเหมารวมว่า sync ทุกช่องทางพัง

### 7. ปุ่มล็อกถูกตัดออกบนจอมือถือ — กลางถึงสูง

ภาพ 390 × 844 แสดงแถว Header ล้นด้านขวา ปุ่มล็อกเริ่ม x≈386.65 และสิ้นสุด x≈420.25 ใน viewport กว้าง 390 จึงแตะได้ยากมากผ่านหน้าจอจริง โค้ดอยู่ src/components/Header.jsx:61 เป็นต้นไป
แก้: คงปุ่มล็อกและเพิ่มรายการเป็นปุ่มหลัก ย้ายพิมพ์/ธีม/สุ่มรหัสไปเมนูเพิ่มเติม ทดสอบ 320, 360, 390, 430 px พร้อมคีย์บอร์ดเปิด แนวตั้ง/แนวนอน และ safe areas

## ปัญหาเพิ่มเติมจากการอ่านโค้ด

- ปิด Firebase ไม่ถาวร: src/services/storage.js:83 ใช้ parsed.firebaseConfig || DEFAULT_FIREBASE_CONFIG จึงกลับมาเปิดหลัง reload ยืนยันใน isolated test; Firebase instance เดิมก็ไม่ถูก shutdown และ init ใช้ getApp เดิมแม้เปลี่ยน config
- พิมพ์/PDF: src/components/PrintVaultModal.jsx:31 ครอบกระดาษ preview ด้วย no-print แต่ src/index.css:95 ซ่อนทั้ง subtree ขณะพิมพ์ จึงคาดว่าจะไม่ออกตารางตาม preview ต้องแก้โครงสร้างและตรวจ print output จริง
- Clipboard: ตัวสุ่มรหัสใช้ navigator.clipboard โดยตรง ไม่ผ่าน auto-wipe service; ปุ่มคัดลอกหลายแห่งไม่ await ผลก่อนแสดงสำเร็จ service กลืน error การอ่านเพื่อล้าง clipboard การทดสอบ browser tool ยังยืนยันเนื้อหา clipboard/การล้างครบวงจรไม่ได้
- Lock: เคลียร์ active key และ vaultItems แต่ selectedItem ใน App ยังเก็บ object เดิมไว้ใน React state ควรล้าง state ที่ถือ plaintext ทั้งหมด ปัจจุบันไม่มี visibilitychange/appStateChange handler สำหรับล็อกเมื่อพักแอป
- Android: MainActivity เป็น BridgeActivity เปล่า ไม่พบการป้องกันหน้ารวมแอปล่าสุดด้วย FLAG_SECURE หรือ biometric/Keystore integration; allowBackup=true ควรกำหนด backup rules ให้สอดคล้องกับ key storage
- Password generator ใช้ byte % charset.length ทำให้ distribution มี bias และไม่รับประกันว่าครบทุกชนิดที่เลือก ควรใช้ rejection sampling และบังคับอย่างน้อยหนึ่งอักขระจากแต่ละกลุ่มเมื่อผู้ใช้ต้องการ
- เว็บยังไม่มี service worker/manifest สำหรับ offline PWA; APK มี assets ฝังไว้ แต่ยังไม่ผ่านการลอง offline บนเครื่องจริง
- GitHub มี app-debug.apk แต่ไม่มี release/tag ควรจัด signed release และระบบ version/update; workflow ปัจจุบันมี warning เกี่ยวกับ actions ที่ใช้ Node.js 20
- ยังไม่ได้ตรวจ Firestore deployed security rules จึงไม่สรุปว่าคนอื่นอ่านข้อมูลได้หรือไม่ได้ Firebase web config ที่อยู่ใน source เพียงอย่างเดียวไม่ใช่หลักฐานว่ารหัสผ่านรั่ว

## ลูกเล่นที่แนะนำหลังแก้พื้นฐาน

1. สแกนนิ้ว/ใบหน้าผูก Keystore พร้อมล็อกเมื่อออกจากแอป
2. หน้า “สุขภาพรหัสผ่าน” แจ้งรหัสซ้ำ/สั้น โดยวิเคราะห์ในเครื่อง
3. ประวัติแก้ไขและถังขยะกู้คืน ลดความเสี่ยงเผลอลบหรือเขียนทับ
4. ตัวช่วยสำรอง แสดงสำรองล่าสุด/จำนวนรายการ และทดลองกู้คืนใน sandbox ได้
5. Autofill บน Android พร้อมตรวจเว็บไซต์ที่ตรงกัน
6. ตัวสร้าง passphrase และปุ่ม “ใช้รหัสนี้” ส่งกลับฟอร์มเพิ่มรายการโดยตรง

ลำดับแนะนำ: Recovery/backup/data integrity → PIN/cloud identity → mobile layout/lifecycle → signed release และทดสอบโทรศัพท์จริง → ลูกเล่น
